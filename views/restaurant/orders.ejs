<div class="pt-24">
    <section id="sectionData" class="menu container mx-auto py-8">
<% console.log(orders.length) %>
        <% if(orders.length) { %>
            
            <h1 class="text-center font-bold text-lg mb-4">All Orders</h1>
    
            <% if(messages.success) { %>
                <% console.log("AM I REACHING HERE !") %>

    
                <div class="flex justify-center p-4 mb-6 text-sm text-green-700 bg-green-100 rounded-lg dark:bg-green-200 dark:text-green-800" role="alert" id="success-alert">
                    <svg aria-hidden="true" class="flex-shrink-0 inline w-5 h-5 mr-3" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path></svg>
                    <span class="sr-only">Info</span>
                    <div>
                      <span class="font-medium"><%= messages.success %></span>
                    </div>
                  </div>            
                <br>
                <% } %>
               


<div class="overflow-x-auto relative shadow-md sm:rounded-lg">

<table class="w-full text-lg text-center text-gray-500 dark:text-gray-400">
    <thead class="text-xl text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
        <tr>
            <th scope="col" class="px-0">
                Order ID
            </th>
            <th scope="col" class="px-0">
               Customer Name
            </th>
            <th scope="col" class="px-0">
                Items
            </th>
            <th scope="col" class="px-0">
                Address
            </th>
            <th scope="col" class="px-0">
                Status
            </th>
            <th scope="col" class="px-0">
                Order Date/Time
            </th>
            <th scope="col" class="px-0">
                Payment Type
            </th>
            <th scope="col" class="px-0">
                Delete
            </th>
        </tr>
    </thead>
    <tbody id="orderTableBody">
    </table>

    <% } else { %>

        <h1 class="text-center font-bold text-3xl">No Tacos 🌮 Orders Found!</h1>

            <div class="empty-cart">
                <div class="container mx-auto text-center">
                    <img class="w-2/5 mx-auto" src="/images/noorder.png" alt="empty-cart">
                </div>
            </div>
            <% } %>
           

</section>
</div>


    
